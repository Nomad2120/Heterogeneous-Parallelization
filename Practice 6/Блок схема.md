```mermaid
flowchart TD
    A[старт] --> B[задать размеры массивов]
    B --> C[создать массивы A и B]
    C --> D[заполнить массивы случайными числами]

    D --> E[вычисление на CPU]
    E --> E1[последовательный цикл]
    E1 --> E2[C_cpu = A + B]
    E2 --> E3[замер времени CPU]

    D --> F[вычисление на GPU через OpenCL]
    F --> F1[инициализация OpenCL]
    F1 --> F2[создать контекст и очередь]
    F2 --> F3[загрузить и скомпилировать kernel]
    F3 --> F4[скопировать данные A и B на GPU]
    F4 --> F5[запустить kernel vector_add]
    F5 --> F6[скопировать результат C_gpu на CPU]
    F6 --> F7[замер времени GPU]

    E3 --> G[сравнить результаты]
    F7 --> G
    G --> H{результаты совпадают}

    H -- да --> I[ok_cpu = 1, ok_gpu = 1]
    H -- нет --> J[ошибка результата]

    I --> K[вывести CSV]
    K --> L[построить график CPU vs GPU]
    L --> M[конец]
